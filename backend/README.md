# Household Portfolio Backend

FastAPI backend for the Household Portfolio Tracker.

## Install

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install .[dev]
```

Add `.[analytics]` if you want to install QuantStats for the later analytics phase.
